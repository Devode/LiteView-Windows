<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LiteView.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiteView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LiteView">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  TitleBar  -->
            <RowDefinition Height="*" />
            <!--  NavigationView  -->
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="titleBar"
            Title="LiteView"
            IsPaneToggleButtonVisible="True"
            IsBackButtonVisible="{x:Bind ViewModel.IsBackButtonVisible, Mode=OneWay}"
            BackRequested="titleBar_BackRequested"
            PaneToggleRequested="titleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Icons/LiteViewIcon.ico" />
            </TitleBar.IconSource>
            <TitleBar.Content>
                <AutoSuggestBox x:Name="SearchBox"
                                Width="360"
                                VerticalAlignment="Center"
                                CornerRadius="8"
                                PlaceholderText="搜索"
                                QueryIcon="Find"
                                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
                                ItemsSource="{x:Bind ViewModel.SuggestionItems, Mode=OneWay}"/>
            </TitleBar.Content>
        </TitleBar>

        <NavigationView
            x:Name="navView"
            Grid.Row="1"
            PaneDisplayMode="LeftMinimal"
            IsBackButtonVisible="Collapsed"
            IsPaneOpen="{x:Bind ViewModel.IsPaneOpen, Mode=TwoWay}"
            IsPaneToggleButtonVisible="False"
            SelectionChanged="navView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="PDF 列表" Icon="List" Tag="PdfListPage" />
                <NavigationViewItem Content="PDF 阅读器" Tag="PdfViewerPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE736;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <Frame x:Name="navFrame" />
        </NavigationView>

    </Grid>
</Window>
