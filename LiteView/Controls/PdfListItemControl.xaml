<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LiteView.Controls.PdfListItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiteView.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal">
                    <VisualState.Setters>
                        <Setter Target="CardRoot.Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />

                    </VisualState.Setters>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="DropShadow"
                                         Storyboard.TargetProperty="BlurRadius" To="4"
                                         Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="Card3D"
                                         Storyboard.TargetProperty="TranslateY" To="0"
                                         Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </VisualState>

                <VisualState x:Name="PointerOver">
                    <VisualState.Setters>
                        <Setter Target="CardRoot.Background" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}" />
                    </VisualState.Setters>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="DropShadow"
                                         Storyboard.TargetProperty="BlurRadius" To="16"
                                         Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="Card3D"
                                         Storyboard.TargetProperty="TranslateY" To="-1"
                                         Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        

        <Border x:Name="ShadowTarget" />

        <Border
            x:Name="CardRoot"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16"
            Margin="0,4"
            Translation="0,0,4"
            HorizontalAlignment="Stretch"
            PointerEntered="CardRoot_PointerEntered"
            PointerExited="CardRoot_PointerExited">

            <toolkit:Effects.Shadow>
                <toolkit:AttachedDropShadow x:Name="DropShadow"
                                            CastTo="{x:Bind ShadowTarget}"
                                            CornerRadius="8"
                                            BlurRadius="4"
                                            Color="{ThemeResource CardStrokeColorDefault}"
                                            Offset="0,4" />
            </toolkit:Effects.Shadow>

            <Border.Transform3D>
                <CompositeTransform3D x:Name="Card3D" TranslateZ="4" TranslateY="0" />
            </Border.Transform3D>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <FontIcon Grid.RowSpan="2" Glyph="&#xEA90;" FontSize="32" Foreground="Red" />
                <TextBlock
                    x:Name="fileName"
                    Grid.Column="1"
                    Margin="0,12,0,0"
                    VerticalAlignment="Bottom"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    Text="{x:Bind Model.FileName, Mode=OneWay}"
                    TextLineBounds="TrimToCapHeight"
                    TextWrapping="NoWrap" />
                <TextBlock
                    x:Name="subtitleText"
                    Grid.Row="1"
                    Grid.Column="1"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    RelativePanel.Below="titleText"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    TextTrimming="WordEllipsis">
                    <Run Text="修改时间：" />
                    <Run Text="{x:Bind Model.ModifyTime, Mode=OneWay}" />
                    <Run Text="    文件路径：" />
                    <Run Text="{x:Bind Model.FilePath, Mode=OneWay}" />
                    <ToolTipService.ToolTip>
                        <ToolTip Visibility="{Binding ElementName=subtitleText, Path=IsTextTrimmed}">
                            <TextBlock Text="{x:Bind Model.FilePath, Mode=OneWay}" TextWrapping="Wrap" />
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </TextBlock>
            </Grid>

            <!-- <Border.Resources>
                <Storyboard x:Name="RiseStoryBoard">
                    <DoubleAnimation
                        Storyboard.TargetName="CardRoot"
                        Storyboard.TargetProperty="(UIElement.Translation).X"
                        To="0"
                        Duration="0:0:1" />
                    <DoubleAnimation
                        Storyboard.TargetName="CardRoot"
                        Storyboard.TargetProperty="(UIElement.Translation).Y"
                        To="-4"
                        Duration="0:0:1" />
                    <DoubleAnimation
                        Storyboard.TargetName="CardRoot"
                        Storyboard.TargetProperty="(UIElement.Translation).Z"
                        To="16"
                        Duration="0:0:1" />
                </Storyboard>
            </Border.Resources> -->
        </Border>
    </Grid>
</UserControl>
