<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LiteView.Controls.PdfViewerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiteView.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:LiteView.Models"
    xmlns:control="using:LiteView.Controls"
    mc:Ignorable="d">

    <ScrollViewer x:Name="PdfScrollViewer" ZoomMode="Enabled" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled"
                  IsHorizontalRailEnabled="True" IsVerticalRailEnabled="True" IsScrollInertiaEnabled="True"
                  ViewChanged="PdfScrollViewer_ViewChanged">
        <Grid x:Name="ViewerRootGrid">
            <control:AnnotationCanvasControl x:Name="AnnotationCanvas"
                                             IsHitTestVisible="False" />
            
            <ItemsRepeater x:Name="PdfPagesRepeater"
                           Canvas.ZIndex="0">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Vertical" Spacing="10" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="model:PdfPageViewModel">
                        <Border Width="{x:Bind PageWidth}" 
                                Height="{x:Bind PageHeight}" 
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                Translation="0,0,16">
                            <Border.Shadow>
                                <ThemeShadow />
                            </Border.Shadow>

                            <Image Source="{x:Bind PageImage, Mode=OneWay}"
                                   Stretch="Uniform" />

                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>

            <Canvas x:Name="PartialRenderCanvas" Background="Transparent"
                    IsHitTestVisible="False" >
                <Image x:Name="ParticalImageTop"
                       Visibility="Collapsed"
                       IsHitTestVisible="False"
                       Stretch="Fill" />
                <Image x:Name="ParticalImageBottom"
                       Visibility="Collapsed"
                       IsHitTestVisible="False"
                       Stretch="Fill" />
            </Canvas>
        </Grid>

        

    </ScrollViewer>
</UserControl>
