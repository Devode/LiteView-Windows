<ResourceDictionary
    x:Class="LiteView.Styles.PdfItemTemplates"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:LiteView.Models">

    <DataTemplate x:Key="ControlPdfItemTemplate" x:DataType="models:PdfItem">
        <Border
            x:Name="CardRoot"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16"
            Margin="0,4"
            Translation="0,0,4"
            HorizontalAlignment="Stretch"
            PointerEntered="CardRoot_PointerEntered"
            PointerExited="CardRoot_PointerExited">

            <Border.TranslationTransition>
                <Vector3Transition />
            </Border.TranslationTransition>

            <!-- <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />

                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Target="CardRoot.Background" Value="{ThemeResource CardBackgroundFillColorSecondaryBrush}" />
                            <Setter Target="CardRoot.Translation" Value="0,4,16" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups> -->

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <FontIcon Grid.RowSpan="2" Glyph="&#xEA90;" FontSize="32" Foreground="Red" />
                <TextBlock
                        x:Name="fileName"
                        Grid.Column="1"
                        Margin="0,12,0,0"
                        VerticalAlignment="Bottom"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="{x:Bind FileName}"
                        TextLineBounds="TrimToCapHeight"
                        TextWrapping="NoWrap" />
                <TextBlock
                    x:Name="subtitleText"
                    Grid.Row="1"
                    Grid.Column="1"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    RelativePanel.Below="titleText"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    TextTrimming="WordEllipsis">
                    <Run Text="修改时间：" />
                    <Run Text="{x:Bind ModifyTime}" />
                    <Run Text="    文件路径：" />
                    <Run Text="{x:Bind FilePath}" />
                    <ToolTipService.ToolTip>
                        <ToolTip Visibility="{Binding ElementName=subtitleText, Path=IsTextTrimmed}">
                            <TextBlock Text="{x:Bind FilePath}" TextWrapping="Wrap" />
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </TextBlock>
            </Grid>
            
        </Border>
    </DataTemplate>
</ResourceDictionary>
